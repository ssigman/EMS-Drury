To create the data base run the following command from the bash command line:
`mongoimport --db university --collection courses --file courses.json`

Note this command will not work from the mongo shell.